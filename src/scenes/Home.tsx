import { Box } from '@mui/system';
import Hero from '../components/hero/Hero';

const Home = () => {
  return (
    <Box
      sx={{
        margin: '1rem',
      }}
    >
      {' '}
      <Hero />
    </Box>
  );
};

export default Home;
